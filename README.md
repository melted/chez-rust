Workspace for Chez Scheme Rust bindings.

`chez-sys` is the low-level bindings, providing a Rust library with the raw C API.

`chez-scheme` will provide high-level bindings, but there is no implementation yet.